# CI workflow - currently disabled
# Uncomment below to enable automatic flake checks on PRs and commits

# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   check-flake:
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Install Nix
#         uses: DeterminateSystems/nix-installer-action@main
#         with:
#           extra-conf: |
#             experimental-features = nix-command flakes
#
#       - name: Setup Nix cache
#         uses: DeterminateSystems/magic-nix-cache-action@main

#       - name: Check flake
#         run: nix flake check -L

#       - name: Verify NixOS configuration builds
#         run: nix build .#nixosConfigurations.kyun-cloud-init.config.system.build.toplevel -L --no-link
